import React from 'react';
import ReactDOM from 'react-dom/client';
import EvolveApp from './EvolveApp.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <EvolveApp />
  </React.StrictMode>
);
